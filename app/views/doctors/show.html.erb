<%= @doctor.name %>
<%= @doctor.department %>

<h1>Appointments</h1>

<ul>
  <%= @doctor.appointments.each do |appt| %>
    <li>
    <%= link_to appt.patient.name, patient_path(appt.patient) %> - <%= appt.appointment_datetime.strftime("%B %d, %Y at %k:%M") %>
    </li>
  <% end %>
  </ul>