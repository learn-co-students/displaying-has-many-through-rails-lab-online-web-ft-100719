<h2><%= @patient.name %></h2>
<p>
  Age: <%= @patient.age %>
</p>
Appointments: <br>
  <% @patient.appointments.each do |appointment| %>
    <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %> at
    <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M ") %>
    <br>
  <% end %>