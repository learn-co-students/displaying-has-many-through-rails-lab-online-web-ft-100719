<h1> @patient.name</h1>
<% @patient.appointments.each do |appointment| %>
    <li> <%= appointment.appointment_datetime.strftime("%B %d, %Y at %H:%M") %> </li> <br>
    <%= link_to appointment.doctor.name, doctor_path(appointment.doctor) %><br>
<% end %>