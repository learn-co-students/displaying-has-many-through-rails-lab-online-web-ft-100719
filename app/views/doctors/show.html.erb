<h1><%= @doctor.name %>'s Appointments </h1>

<ul>
<% @doctor.appointments.each do |app| %>
  <li><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M")%><br>
      <a href="/patients/<%= app.patient.id %>"><%= app.patient.name %>
  </li>
<% end %>
</ul>
