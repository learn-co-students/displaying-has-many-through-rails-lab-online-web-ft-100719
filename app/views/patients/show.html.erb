<h1><%= @patient.name %>'s Appointments </h1>

<ul>
<% @patient.appointments.each do |app| %>
  <li><%= app.appointment_datetime.strftime("%B %d, %Y at %H:%M")%><br>
      <a href="/doctors/<%= app.doctor.id %>"><%= app.doctor.name %>
  </li>
<% end %>
</ul>
